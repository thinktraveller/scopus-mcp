# 🤖 Scopus MCP 使用指南与提示词示例

这份指南展示了如何通过自然语言对话，触发 Scopus MCP Server 的各项功能。您可以直接将下列提示词（Prompts）发送给 Claude、Trae 或其他支持 MCP 的 AI 助手。

## 场景一：文献检索与趋势分析 (Research & Trends)

想要了解某个领域的最新进展。

**用户输入 (User Prompt):**
> "帮我搜索关于 'Large Language Models' 在 'Healthcare' 领域的最新论文。请找 2023 年以后发表的，按被引量排序，并总结一下目前的主要研究趋势。"

**🔍 触发工具 (Triggered Tool):**
1.  `search_scopus(query="TITLE-ABS-KEY(Large Language Models AND Healthcare) AND PUBYEAR > 2022", sort="citedby-count", count=5)`
2.  *(LLM 会阅读返回的 JSON 结果并为您撰写总结)*

---

## 场景二：顺藤摸瓜找论文 (Citation Chaining)

找到一篇核心论文后，想看看谁引用了它（查找此后的研究）。

**用户输入 (User Prompt):**
> "我有这篇论文的 Scopus ID: 85190624599。请帮我查一下它的摘要。然后，帮我看看**有哪些新论文引用了它**，我想看看后续的发展。"

**🔍 触发工具 (Triggered Tool):**
1.  `get_abstract_details(scopus_id="85190624599")`
2.  `get_citing_papers(scopus_id="85190624599", sort="coverDate")`

---

## 场景三：学者影响力分析 (Author Profiling)

分析某个教授的学术背景。

**用户输入 (User Prompt):**
> "请帮我查找 Scopus 作者 ID 为 '57201017345' 的详细资料。我想知道他目前的归属机构、总引用量以及 H-index 情况。"

**🔍 触发工具 (Triggered Tool):**
1.  `get_author_profile(author_id="57201017345")`

---

## 场景四：自我检查 (Quota Check)

当你不确定 API 额度是否够用时。

**用户输入 (User Prompt):**
> "检查一下我的 Scopus API 额度还剩多少？"

**🔍 触发工具 (Triggered Tool):**
1.  `get_quota_status()`

---

## 🤖 使用预置 Prompts (System Prompts)

这个 MCP Server 内置了两个强大的预设 Prompt，您可以直接调用：

1.  **一键生成研究综述**
    *   **Prompt Name:** `research-summary`
    *   **使用方式:** (在支持 Prompt 选择的 UI 中选择此项，或告诉 AI "使用 research-summary 模板")
    *   **参数:** `topic` (例如: "Autonomous Driving Safety")
    *   **效果:** 自动执行搜索、排序、并生成结构化的综述报告。

2.  **一键作者分析**
    *   **Prompt Name:** `author-analysis`
    *   **使用方式:** (选择此项)
    *   **参数:** `author_id`
    *   **效果:** 自动拉取作者信息并分析其学术生涯亮点。

## 💡 高级搜索技巧 (Search Syntax)

在与 LLM 交流时，您可以指导它使用更精准的 Scopus 搜索语法：

*   **指定字段:** `TITLE(AI)`, `AUTH(Smith)`
*   **逻辑组合:** `AND`, `OR`, `AND NOT`
*   **时间:** `PUBYEAR > 2020`
*   **短语:** `"{exact phrase}"`
